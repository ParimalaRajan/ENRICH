#include<stdio.h>

int main() 
{
    int a,count=0;
    scanf("%d",&a);
    while(a>0)
    {
        if(a&1)
        {
            count=count+1;
        }
        a=a/2;
    }
    printf("%d",count);
}
